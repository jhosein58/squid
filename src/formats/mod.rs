pub mod wav;

// use crate::buffer::AudioBufferML;
// use crate::error::Result;
// use std::path::Path;

// pub trait AudioFormat {
//     fn from_path<P: AsRef<Path>>(path: P) -> Result<Self>;
//     fn write_to_path<P: AsRef<Path>>(buffer: &Self, path: P) -> Result<()>;
// }
